export enum RolesType {
    ADMIN = 'admin',
    USER = 'user',
    GUEST = 'guest',
}

export const ROLES_KEY = 'roles';

export enum Sort {
  ASC= 'ASC',
  DESC= 'DESC',
}

export enum StudentType {
  THI = 'thi',
  ON = 'ôn',
  ALL = 'ôn + thi',
}